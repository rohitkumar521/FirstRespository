May 12 vide
ls
rm -rm * (remove all data)
mkdir dir1
touch file1
chmd (permissions)




tar -cvf dira.tar dira  c- create v- verboles f-forcefully
gzip dira.tar
rm -rf (remove file)
gunzip dira.tar.gz
tar -xvf dira.tar
wget (download)
yum (install)  
ansible class t know about users and admin part
